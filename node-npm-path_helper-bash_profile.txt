Node.js is installed together with Npm.
Extract binaries to ~/opt/packages/node-v6

$~/opt/packages/node-v6

mkdir -p ~/opt/packages && cd $_
Create children folders and change directory to expanded folder

$ node -- version
$ npm -- version
$ npm install - -global npm@latest

If npm cannot be found ->>>
sudo chmod -R u=rwx,g=rwx,o=rx lib

$sudo man path_helper
/etc/paths
/etc/paths.d

Better idea
In .bash_profile, source .profile. Comment the source statment to recover from mistakes

.bash_profile >>>
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_131.jdk/Contents/Home
export JAVA_HOME

ANT_HOME=/Library/apache-ant-1.10.1
export ANT_HOME

MAVEN_HOME=/Library/apache-maven-3.5.0
export MAVEN_HOME

# Set NODE_HOME configure NPM installation default directory.
NODE_HOME=/Users/matt/opt/packages/node-v6
export NODE_HOME

export PATH=${NODE_HOME}/bin:${MAVEN_HOME}/bin:${ANT_HOME}/bin:${JAVA_HOME}/bin:${PATH}

pip install -r requirements.txt -> install everything in the requirements 